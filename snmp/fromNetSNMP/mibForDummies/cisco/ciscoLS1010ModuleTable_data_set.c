/*
 * Note: this file originally auto-generated by mib2c
 * using mfd-data-set.m2c
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-features.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "ciscoLS1010ModuleTable.h"


/** @defgroup data_set data_set: Routines to set data
 *
 * These routines are used to set the value for individual objects. The
 * row context is passed, along with the new value.
 * 
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table ciscoLS1010ModuleTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * CISCO-RHINO-MIB::ciscoLS1010ModuleTable is subid 1 of ciscoLS1010ModuleGroup.
 * Its status is Current.
 * OID: .1.3.6.1.4.1.9.5.11.1.2.1, length: 12
*/
    /*
     * NOTE: if you update this chart, please update the versions in
     *       local/mib2c-conf.d/parent-set.m2i
     *       agent/mibgroup/helpers/baby_steps.c
     * while you're at it.
     */
    /*
     ***********************************************************************
     * Baby Steps Flow Chart (2004.06.05)                                  *
     *                                                                     *
     * +--------------+    +================+    U = unconditional path    *
     * |optional state|    ||required state||    S = path for success      *
     * +--------------+    +================+    E = path for error        *
     ***********************************************************************
     *
     *                        +--------------+
     *                        |     pre      |
     *                        |   request    |
     *                        +--------------+
     *                               | U
     *                        +==============+
     *       +----------------||  object    ||
     *       |              E ||  lookup    ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |              E ||   check    ||
     *       |<---------------||   values   ||
     *       |                +==============+
     *       |                       | S
     *       |                +==============+
     *       |       +<-------||   undo     ||
     *       |       |      E ||   setup    ||
     *       |       |        +==============+
     *       |       |               | S
     *       |       |        +==============+
     *       |       |        ||    set     ||-------------------------->+
     *       |       |        ||   value    || E                         |
     *       |       |        +==============+                           |
     *       |       |               | S                                 |
     *       |       |        +--------------+                           |
     *       |       |        |    check     |-------------------------->|
     *       |       |        |  consistency | E                         |
     *       |       |        +--------------+                           |
     *       |       |               | S                                 |
     *       |       |        +==============+         +==============+  |
     *       |       |        ||   commit   ||-------->||     undo   ||  |
     *       |       |        ||            || E       ||    commit  ||  |
     *       |       |        +==============+         +==============+  |
     *       |       |               | S                     U |<--------+
     *       |       |        +--------------+         +==============+
     *       |       |        | irreversible |         ||    undo    ||
     *       |       |        |    commit    |         ||     set    ||
     *       |       |        +--------------+         +==============+
     *       |       |               | U                     U |
     *       |       +-------------->|<------------------------+
     *       |                +==============+
     *       |                ||   undo     ||
     *       |                ||  cleanup   ||
     *       |                +==============+
     *       +---------------------->| U
     *                        +--------------+
     *                        |    post      |
     *                        |   request    |
     *                        +--------------+
     *
     */

/**
 * Setup up context with information needed to undo a set request.
 *
 * This function will be called before the individual node undo setup
 * functions are called. If you need to do any undo setup that is not
 * related to a specific column, you can do it here.
 *
 * Note that the undo context has been allocated with
 * ciscoLS1010ModuleTable_allocate_data(), but may need extra
 * initialization similar to what you may have done in
 * ciscoLS1010ModuleTable_rowreq_ctx_init().
 * Note that an individual node's undo_setup function will only be called
 * if that node is being set to a new value.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in the node's undo_setup
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (ciscoLS1010ModuleTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
ciscoLS1010ModuleTable_undo_setup( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleTable_undo_setup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> Setup ciscoLS1010ModuleTable undo.
     * set up ciscoLS1010ModuleTable undo information, in preparation for a set.
     * Undo storage is in (* ciscoLS1010ModuleSerialNumberString_val_ptr_ptr )*
     */

    return rc;
} /* ciscoLS1010ModuleTable_undo_setup */

/**
 * Undo a set request.
 *
 * This function will be called before the individual node undo
 * functions are called. If you need to do any undo that is not
 * related to a specific column, you can do it here.
 *
 * Note that an individual node's undo function will only be called
 * if that node is being set to a new value.
 *
 * If there is anything  specific to a particular column (e.g. releasing
 * memory for a string), you should do that setup in the node's undo
 * function, so it won't be done unless it is necessary.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (ciscoLS1010ModuleTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 */
int
ciscoLS1010ModuleTable_undo( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleTable_undo","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:451:M: |-> ciscoLS1010ModuleTable undo.
     * ciscoLS1010ModuleTable undo information, in response to a failed set.
     * Undo storage is in (* ciscoLS1010ModuleSerialNumberString_val_ptr_ptr )*
     */

    return rc;
} /* ciscoLS1010ModuleTable_undo_setup */

/**
 * Cleanup up context undo information.
 *
 * This function will be called after set/commit processing. If you
 * allocated any resources in undo_setup, this is the place to release
 * those resources.
 *
 * This function is called regardless of the success or failure of the set
 * request. If you need to perform different steps for cleanup depending
 * on success or failure, you can add a flag to the rowreq_ctx.
 *
 * @param rowreq_ctx
 *        Pointer to the table context (ciscoLS1010ModuleTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
ciscoLS1010ModuleTable_undo_cleanup( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleTable_undo_cleanup","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:452:M: |-> Cleanup ciscoLS1010ModuleTable undo.
     * Undo storage is in (* ciscoLS1010ModuleSerialNumberString_val_ptr_ptr )*
     */

    return rc;
} /* ciscoLS1010ModuleTable_undo_cleanup */

/**
 * commit new values.
 *
 * At this point, you should have done everything you can to ensure that
 * this commit will not fail.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * ciscoLS1010ModuleTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param ciscoLS1010ModuleTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
ciscoLS1010ModuleTable_commit( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;
    int             save_flags;

    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleTable_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * save flags, then clear until we actually do something
     */
    save_flags = rowreq_ctx->column_set_flags;
    rowreq_ctx->column_set_flags = 0;

    /*
     * commit ciscoLS1010ModuleTable data
     * 1) check the column's flag in save_flags to see if it was set.
     * 2) clear the flag when you handle that column
     * 3) set the column's flag in column_set_flags if it needs undo
     *    processing in case of a failure.
     */
    if (save_flags & COLUMN_CISCOLS1010MODULEADMINSTATUS_FLAG) {
       save_flags &= ~COLUMN_CISCOLS1010MODULEADMINSTATUS_FLAG; /* clear ciscoLS1010ModuleAdminStatus */
       /*
        * TODO:482:o: |-> commit column ciscoLS1010ModuleAdminStatus.
        */
       rc = -1;
       if(-1 == rc) {
           snmp_log(LOG_ERR,"ciscoLS1010ModuleTable column ciscoLS1010ModuleAdminStatus commit failed\n");
       }
       else {
            /*
             * set flag, in case we need to undo ciscoLS1010ModuleAdminStatus
             */
            rowreq_ctx->column_set_flags |= COLUMN_CISCOLS1010MODULEADMINSTATUS_FLAG;
       }
    }

    /*
     * if we successfully commited this row, set the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags |= MFD_ROW_DIRTY;
    }

    if (save_flags) {
       snmp_log(LOG_ERR, "unhandled columns (0x%x) in commit\n", save_flags);
       return MFD_ERROR;
    }

    return rc;
} /* ciscoLS1010ModuleTable_commit */

/**
 * undo commit new values.
 *
 * Should you need different behavior depending on which columns were
 * set, rowreq_ctx->column_set_flags will indicate which writeable columns were
 * set. The definitions for the COLUMN_*_FLAG bits can be found in
 * ciscoLS1010ModuleTable_oids.h.
 * A new row will have the MFD_ROW_CREATED bit set in rowreq_flags.
 *
 * @param ciscoLS1010ModuleTable_rowreq_ctx
 *        Pointer to the users context.
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error
 */
int
ciscoLS1010ModuleTable_undo_commit( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx)
{
    int rc = MFD_SUCCESS;

    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleTable_undo_commit","called\n"));

    /** we should have a non-NULL pointer */
    netsnmp_assert( NULL != rowreq_ctx );

    /*
     * TODO:485:M: |-> Undo ciscoLS1010ModuleTable commit.
     * check each column's flag in rowreq_ctx->column_set_flags to see
     * if it was set during commit, then undo it.
     *
     * eg: if (rowreq_ctx->column_set_flags & COLUMN_CISCOLS1010MODULEADMINSTATUS_FLAG) {}
     */

    
    /*
     * if we successfully un-commited this row, clear the dirty flag.
     */
    if (MFD_SUCCESS == rc) {
        rowreq_ctx->rowreq_flags &= ~MFD_ROW_DIRTY;
    }

    return rc;
} /* ciscoLS1010ModuleTable_undo_commit */

/*
 * TODO:440:M: Implement ciscoLS1010ModuleTable node value checks.
 * TODO:450:M: Implement ciscoLS1010ModuleTable undo functions.
 * TODO:460:M: Implement ciscoLS1010ModuleTable set functions.
 * TODO:480:M: Implement ciscoLS1010ModuleTable commit functions.
 */
/*---------------------------------------------------------------------
 * CISCO-RHINO-MIB::ciscoLS1010ModuleEntry.ciscoLS1010ModuleAdminStatus
 * ciscoLS1010ModuleAdminStatus is subid 8 of ciscoLS1010ModuleEntry.
 * Its status is Current, and its access level is ReadWrite.
 * OID: .1.3.6.1.4.1.9.5.11.1.2.1.1.8
 * Description:
This object, when read, always returns a value of
                      ok(1).  Setting this object to one of the
                      acceptable values gives the following results:
                          reset:   reset the module's control logic
                          enable:  enable the module
                          disable: disable the module
                      Setting this object to any other values results in 
                      an error.
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  1      hasdefval 0
 *   readable   1     iscolumn 1     ranges 0      hashint   0
 *   settable   1
 *
 * Enum range: 3/8. Values:  enable(1), disable(2), reset(3)
 *
 * Its syntax is AdminStatus (based on perltype INTEGER)
 * The net-snmp type is ASN_INTEGER. The C type decl is long (u_long)
 */
/**
 * Check that the proposed new value is potentially valid.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param ciscoLS1010ModuleAdminStatus_val
 *        A long containing the new value.
 *
 * @retval MFD_SUCCESS        : incoming value is legal
 * @retval MFD_NOT_VALID_NOW  : incoming value is not valid now
 * @retval MFD_NOT_VALID_EVER : incoming value is never valid
 *
 * This is the place to check for requirements that are not
 * expressed in the mib syntax (for example, a requirement that
 * is detailed in the description for an object).
 *
 * You should check that the requested change between the undo value and the
 * new value is legal (ie, the transistion from one value to another
 * is legal).
 *      
 *@note
 * This check is only to determine if the new value
 * is \b potentially valid. This is the first check of many, and
 * is one of the simplest ones.
 * 
 *@note
 * this is not the place to do any checks for values
 * which depend on some other value in the mib. Those
 * types of checks should be done in the
 * ciscoLS1010ModuleTable_check_dependencies() function.
 *
 * The following checks have already been done for you:
 *    The syntax is ASN_INTEGER
 *    The value is one of  enable(1), disable(2), reset(3)
 *
 * If there a no other checks you need to do, simply return MFD_SUCCESS.
 *
 */
int
ciscoLS1010ModuleAdminStatus_check_value( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx, u_long ciscoLS1010ModuleAdminStatus_val)
{
    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleAdminStatus_check_value","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:441:o: |-> Check for valid ciscoLS1010ModuleAdminStatus value.
     */

    return MFD_SUCCESS; /* ciscoLS1010ModuleAdminStatus value not illegal */
} /* ciscoLS1010ModuleAdminStatus_check_value */

/**
 * Save old value information
 *
 * @param rowreq_ctx
 *        Pointer to the table context (ciscoLS1010ModuleTable_rowreq_ctx)
 *
 * @retval MFD_SUCCESS : success
 * @retval MFD_ERROR   : error. set will fail.
 *
 * This function will be called after the table level undo setup function
 * ciscoLS1010ModuleTable_undo_setup has been called.
 *
 *@note
 * this function will only be called if a new value is set for this column.
 *
 * If there is any setup specific to a particular column (e.g. allocating
 * memory for a string), you should do that setup in this function, so it
 * won't be done unless it is necessary.
 */
int
ciscoLS1010ModuleAdminStatus_undo_setup( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleAdminStatus_undo_setup","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:455:o: |-> Setup ciscoLS1010ModuleAdminStatus undo.
     */
    /*
     * copy ciscoLS1010ModuleAdminStatus data
     * set rowreq_ctx->undo->ciscoLS1010ModuleAdminStatus from rowreq_ctx->data.ciscoLS1010ModuleAdminStatus
     */
    rowreq_ctx->undo->ciscoLS1010ModuleAdminStatus = rowreq_ctx->data.ciscoLS1010ModuleAdminStatus;


    return MFD_SUCCESS;
} /* ciscoLS1010ModuleAdminStatus_undo_setup */

/**
 * Set the new value.
 *
 * @param rowreq_ctx
 *        Pointer to the users context. You should know how to
 *        manipulate the value from this object.
 * @param ciscoLS1010ModuleAdminStatus_val
 *        A long containing the new value.
 */
int
ciscoLS1010ModuleAdminStatus_set( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx, u_long ciscoLS1010ModuleAdminStatus_val )
{

    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleAdminStatus_set","called\n"));

    /** should never get a NULL pointer */
    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:461:M: |-> Set ciscoLS1010ModuleAdminStatus value.
     * set ciscoLS1010ModuleAdminStatus value in rowreq_ctx->data
     */
    rowreq_ctx->data.ciscoLS1010ModuleAdminStatus = ciscoLS1010ModuleAdminStatus_val;

    return MFD_SUCCESS;
} /* ciscoLS1010ModuleAdminStatus_set */

/**
 * undo the previous set.
 *
 * @param rowreq_ctx
 *        Pointer to the users context.
 */
int
ciscoLS1010ModuleAdminStatus_undo( ciscoLS1010ModuleTable_rowreq_ctx *rowreq_ctx)
{

    DEBUGMSGTL(("verbose:ciscoLS1010ModuleTable:ciscoLS1010ModuleAdminStatus_undo","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

    /*
     * TODO:456:o: |-> Clean up ciscoLS1010ModuleAdminStatus undo.
     */
    /*
     * copy ciscoLS1010ModuleAdminStatus data
     * set rowreq_ctx->data.ciscoLS1010ModuleAdminStatus from rowreq_ctx->undo->ciscoLS1010ModuleAdminStatus
     */
    rowreq_ctx->data.ciscoLS1010ModuleAdminStatus = rowreq_ctx->undo->ciscoLS1010ModuleAdminStatus;

    
    return MFD_SUCCESS;
} /* ciscoLS1010ModuleAdminStatus_undo */

/** @} */
